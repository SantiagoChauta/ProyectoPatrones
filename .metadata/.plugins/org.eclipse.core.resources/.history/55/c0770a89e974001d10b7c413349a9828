package com.cliente.DAO;

import com.cliente.Objetos.Cliente;

public interface ObjetoDAO {
	public ObjetoDAO findById(Long id);
	public List<ObjetoDAO> findAll();
	public ObjetoDAO remove(Long id);
	public void Save(Cliente cliente);
	
}
